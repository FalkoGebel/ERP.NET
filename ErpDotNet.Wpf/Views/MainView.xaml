<Window x:Class="ErpDotNet.Wpf.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ErpDotNet.Wpf"
        xmlns:vm="clr-namespace:ErpDotNet.Wpf.ViewModels"
        xmlns:texts="clr-namespace:ErpDotNet.Wpf.Texts"
        xmlns:lists="clr-namespace:ErpDotNet.Wpf.UserControls.Lists"
        xmlns:cards="clr-namespace:ErpDotNet.Wpf.UserControls.Cards"
        mc:Ignorable="d"
        Title="{x:Static texts:MainView.View_Title}"
        d:DesignHeight="450" d:DesignWidth="800"
        WindowState="Maximized">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Left">
                <Button Content="Create Database" Margin="5" Padding="5" Command="{Binding CreateDatabaseCommand}" />
                <Button Content="Open Database" Margin="5" Padding="5" Command="{Binding OpenDatabaseCommand}" />
            </StackPanel>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Vertical">
                <Button Content="Items" Margin="5" Padding="5" Command="{Binding GetItemsFromDatabaseCommand}" />
            </StackPanel>
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            <lists:GeneralList Grid.Column="2"
                               Type="{Binding ListType}"
                               Lines="{Binding Lines}"
                               SelectedIndex="{Binding GeneralListSelectedIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                               Visibility="{Binding GeneralListVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                               HorizontalContentAlignment="Stretch"
                               OpenCard="{Binding OpenCardCommand}"/>
            <cards:ItemCard Grid.Column="2"
                               Item="{Binding CurrentItem}"
                               Visibility="{Binding ItemCardVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                               HorizontalContentAlignment="Stretch"
                               Ok="{Binding ItemCardOkCommand}"
                               Cancel="{Binding ItemCardCancelCommand}"/>

        </Grid>
        <TextBox Grid.Row="2" Text="{Binding StatusMessage, UpdateSourceTrigger=PropertyChanged}" Padding="5" IsEnabled="False"/>
    </Grid>
</Window>
