<UserControl x:Class="ErpDotNet.Wpf.UserControls.Lists.GeneralList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ErpDotNet.Wpf.UserControls.Lists"
             xmlns:texts="clr-namespace:ErpDotNet.Wpf.Texts"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=local:GeneralList, IsDesignTimeCreatable=True}">
    <Grid>
        <ListView x:Name="listView"
                  ItemsSource="{Binding Lines}"
                  Visibility="{Binding ListVisibility}"
                  IsVisibleChanged="ListView_IsVisibleChanged" 
                  Margin="5" />
    </Grid>
</UserControl>