<UserControl x:Class="ErpDotNet.Wpf.UserControls.Lists.ItemList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ErpDotNet.Wpf.UserControls.Lists"
             xmlns:texts="clr-namespace:ErpDotNet.Wpf.Texts"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=local:ItemList, IsDesignTimeCreatable=True}">
    <Grid>
        <ListView ItemsSource="{Binding Items}" Margin="5">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="{x:Static texts:Item.Number}" DisplayMemberBinding="{Binding Number}" Width="Auto"/>
                    <GridViewColumn Header="{x:Static texts:Item.Description}" DisplayMemberBinding="{Binding Description}" Width="Auto"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>